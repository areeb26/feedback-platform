{
  "name": "feedback-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "server",
    "web",
    "mobile"
  ],
  "scripts": {
    "test": "npm run build --workspace=packages/shared && npm run test --workspace=server && npm run test --workspace=web && npm run test --workspace=@feedback-platform/mobile",
    "dev:server": "npm run dev --workspace=server",
    "dev:web": "npm run dev --workspace=web",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=server && npm run build --workspace=web",
    "docker:config": "docker compose config"
  },
  "license": "ISC"
}
